@startuml
skinparam linetype ortho
skinparam packageStyle rectangle

' --- CONFIGURACIÓN DE ESTILO ---
skinparam PackageFontSize 30
skinparam PackageFontStyle bold
' ---------------------------------------

hide empty members

class InputDriver {
    + next() : Request
}

class App {
    --
    + App()
    + {static} main(args : String[])
    + init(inputFile : String)
}

package AppExceptions #LightCoral {
    class AppException
}

package Services #SkyBlue {
    class TicketService {
        --
        + TicketService(cashiers : UserRegister<Cashier>)
        + getTicketList(tickets : List<Ticket<?>>) : String
        + getTickets() : List<Ticket<?>>
        + getTicketsWith(product : BaseProduct<?>) : List<Ticket<?>>
        + showModifiedTickets(product : BaseProduct<?>)
        + getNewTicketID() : TicketID
    }
}

package Persistence #Khaki {
    class PersistenceService {
        --
        + saveAll(catalogData : HashMap<?, ?>, customerData : HashMap<?, ?>, cashierData : HashMap<?, ?>)
        + loadAll() : Object[]
    }

    class "FileHelper<T>" as FileHelper << (S,#FF7700) static >> {
        + save(filename : String, data : T)
        + load(filename : String) : T
    }

    PersistenceService +-- FileHelper
}

package Commands #Plum {
    interface Command {
        + execute(args)
        + help() : String
    }
    Command ..> AppExceptions.AppException : throws

    package User as UserCmd {
        class AddCashier
        class RemoveCashier
        class AddCustomer
        class RemoveUser
        class ListUsers
    }
    UserCmd ..|> Command

    package Product as ProdCmd {
        class AddProduct
        class UpdateProduct
        class RemoveProduct
        class ListProducts
    }
    ProdCmd ..|> Command

    package Ticket as TickCmd {
        class AddNewTicket
        class AddProductToTicket
        class RemoveProductFromTicket
        class PrintTicket
        class ListTickets
    }
    TickCmd ..|> Command
}

package Handlers #Thistle {
    class Request {
        + handlerId : String
        + commandId : String
        + args : String[]
    }
    abstract class RequestHandler {
        + HANDLER_ID : String
        + getCommand(id)
        + getCommands()
    }
    class CashierHandler
    class CustomerHandler
    class ProductHandler
    class TicketHandler

    RequestHandler <|-- CashierHandler
    RequestHandler <|-- CustomerHandler
    RequestHandler <|-- ProductHandler
    RequestHandler <|-- TicketHandler
}

package Ticket #PaleGreen {
    interface PrinterStrategy {
        --
        + init(Ticket<?> ticket) : void
        + printEntry(TicketEntry<?, ?> entry) : String
        + printFooter() : String
        + printHeader() : String
    }

    interface TicketRegistrable {
        --
        + toTicketEntry(String[] args) : TicketEntry
    }

    abstract class Ticket<ProductType extends BaseProduct<?>> {
        --
        + Ticket(ID : TicketID, printerStrat : Supplier<PrinterStrategy>, productType : Class<ProductType>)
        + Ticket(other : Ticket<ProductType>)
        + getID() : TicketID
        + getTicketState() : TicketState
        + hasProduct(productId : ProductID) : boolean
        + iterator() : Iterator<TicketEntry<ProductType, ?>>
        + add(baseProd : T, args : String[]) : int
        + delete(ID : ProductID)
        + update(baseProduct : BaseProduct<?>)
        + close()
        + print()
        + toString() : String
    }

    abstract class TicketEntry

    class CommonTicket extends Ticket
    class CompanyTicket extends Ticket
    class ServiceTicket extends Ticket

    class CommonPrinter implements PrinterStrategy
    class CombinedPrinter implements PrinterStrategy
    class ServicePrinter implements PrinterStrategy

    Ticket *-- TicketEntry
    TicketEntry --> TicketRegistrable : created by
    Ticket *-- PrinterStrategy
}

package Product #PowderBlue {
    abstract class BaseProduct {
        - id : ProductID
    }

    class Catalog {
        --
        + Catalog()
        + loadData(loadedProducts : HashMap<ProductID, BaseProduct<?>>)
        + get(ID : ProductID) : BaseProduct<?>
        + getProducts() : HashMap<ProductID, BaseProduct<?>>
        + add(product : BaseProduct<?>) : int
        + getNewProductID() : ProductID
        + getNewServiceID() : ServiceID
        + delete(ID : ProductID) : BaseProduct<?>
    }

    abstract class GoodsProduct extends BaseProduct {
        - category : Category
        - name : ProductName
        - price : double
    }

    class EventProduct extends GoodsProduct {
        - type : EvenType
        - maxPeople : int
        - expirationData : LocalDate
    }
    class ServiceProduct extends BaseProduct {
        - type : ServiceCategory
        - expirationDate : LocalDate
    }
    class Product extends GoodsProduct {
        - maxPersonalizations : int
    }

    class EventEntry {
        - actualPeople : int
    }
    class ServiceEntry
    class ProductEntry {
        - amount : int
        - personalizations : ArrayList<String>
    }

    Catalog o-- BaseProduct
}

package User #Wheat {
    class Email

    class UserRegister<T extends User> {
        + addUser(user)
        + getUser(id)
        + loadData(data)
        + getRawMap()
    }

    abstract class User {
        # name : String
        # id : String
        --
        + User(id : String, name : String, email : Email)
        + getName() : String
        + getEmail() : Email
        + getId() : String
        + {abstract} toString() : String
    }

    class Cashier extends User
    class Customer extends User {
        - cashID : String
        - tickets : ArrayList<TicketID>
    }

    UserRegister o-- User
    User *-- Email
}

App *-- InputDriver
App *-- PersistenceService
App *-- TicketService
App *-- Product.Catalog
App *-- User.UserRegister
App *-- Handlers.RequestHandler

InputDriver ..> Handlers.Request : creates
Handlers.RequestHandler ..> Commands.Command : generates

TicketService --> User.Cashier : uses
TicketService --> Ticket.Ticket : manages

User.Cashier o-- Ticket.Ticket : owns


Ticket.TicketEntry o-- Product.BaseProduct
Product.EventEntry --|> Ticket.TicketEntry
Product.ServiceEntry --|> Ticket.TicketEntry
Product.ProductEntry --|> Ticket.TicketEntry
Product.BaseProduct --|> Ticket.TicketRegistrable

Command --> TicketService : attacks
Command --> UserRegister : attacks;

@enduml